import React from "react";
import ReactDOM from "react-dom/client";
import EnergyTracker from "./EnergyTracker.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EnergyTracker />
  </React.StrictMode>
);
